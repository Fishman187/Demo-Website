const menu=document.querySelector('.menu');const nav=document.querySelector('nav');const form=document.querySelector('form');const status=document.querySelector('.status');document.querySelector('#year').textContent=new Date().getFullYear();menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.textContent=nav.classList.contains('open')?'×':'☰'});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));form.addEventListener('submit',e=>{e.preventDefault();status.textContent='Thanks — your inquiry is on its way. I’ll be in touch soon.';form.reset()});